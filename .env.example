# Copy to .env.local and fill in. These are PUBLIC client values (safe to ship in
# the browser bundle); real secrets (service role key, PATs) never go in here.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx
