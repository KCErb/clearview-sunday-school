Use for every photograph or artwork. Credit lines on artwork are not optional.

```jsx
<MediaFrame src={url} alt="Christ healing" credit="Carl Heinrich Bloch, Christ Healing the Sick at Bethesda, 1883" />
```

`scrim="bottom"` when text sits over the lower edge; `scrim="side"` for a left-aligned title over a wide image.
