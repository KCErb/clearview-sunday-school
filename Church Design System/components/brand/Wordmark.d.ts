/**
 * Type-only name lockup. NOT the trademarked Church wordmark or symbol — see readme.md.
 * @startingPoint section="Brand" subtitle="Type-only name lockup, light and invert" viewport="700x200"
 */
export interface WordmarkProps { tone?: 'ink' | 'invert'; align?: 'left' | 'center'; scale?: number }
export declare function Wordmark(props: WordmarkProps): JSX.Element;
