Use sparingly, for a decision that must be made before continuing.

```jsx
<Dialog open={open} title="Remove slide?" onClose={close} footer={<Button>Remove</Button>}>
  This slide will be deleted from the lesson.
</Dialog>
```
