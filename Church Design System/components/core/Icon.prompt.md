Use for all UI glyphs. Loads Lucide from CDN and masks it to `currentColor`, so colour comes from the parent.

```jsx
<span style={{ color: 'var(--accent)' }}><Icon name="book-open" size={24} /></span>
```

Never use emoji as an icon. Never use the Church symbol, wordmark or light-ray graphic as an icon.
