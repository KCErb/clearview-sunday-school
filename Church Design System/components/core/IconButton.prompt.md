Use where a glyph alone is unambiguous — close, bookmark, share, next.

```jsx
<IconButton name="bookmark" label="Save to study list" />
```

`name` is a Lucide icon name (see ICONOGRAPHY in readme.md — Lucide is a flagged substitution for the Church's internal icon set).
